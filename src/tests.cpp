#include "tests.h"
#include "inverse_kin.h"

void test()
{
    // Serial.println("unit tests!!!");

    // Coordinate c;
    // c.x = 30;
    // c.y = 0;
    // MotorAngles m = coord_to_angles(c);

    // assert(m.a1 == 0);
    // assert(m.a2 == 0);
}